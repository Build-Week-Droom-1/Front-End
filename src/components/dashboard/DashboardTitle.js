import React from 'react';
import styled from 'styled-components';

const StyledTitle = styled.h1`
    font-size: 4rem;
    color: #caa767;
`;

const DashboardTitle = () => {
    return (
        <StyledTitle>Droom</StyledTitle>
    )
}

export default DashboardTitle;